name: Deploy to Tencent Cloud CVM (Docker)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    # 关键：指定使用的环境（必须和创建的环境名一致）
    environment: production
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '22.12.0'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build

    - name: Copy files to CVM via SCP
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.CVM_HOST }}
        username: ${{ secrets.CVM_USERNAME }}
        key: ${{ secrets.CVM_SSH_KEY }}
        source: "./"
        target: "/myapp/timeline/"

    - name: Deploy via Docker on CVM
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.CVM_HOST }}
        username: ${{ secrets.CVM_USERNAME }}
        key: ${{ secrets.CVM_SSH_KEY }}
        script: |
          cd /myapp/timeline/
          
          # Configure Docker to use amd64 as default platform
          export DOCKER_DEFAULT_PLATFORM=linux/amd64
          
          # Stop and remove existing container
          docker stop timeline-app || true
          docker rm timeline-app || true
          
          # Build new image for AMD64 platform (compatible with Tencent Cloud CVM)
          docker build --platform linux/amd64 -t timeline-app .
          
          # Run container
          docker run -d \
            --name timeline-app \
            -p 80:80 \
            timeline-app
          
          echo "Deployment completed successfully!"